{
  "name": "nodejs",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "prepare": "husky install",
    "prettier": "lint-staged",
    "start": "node src/index.js",
    "dev": "nodemon --inspect src/index.js",
    "build:css": "sass --watch src/resources/scss/:src/public/css/ --no-source-map --style=compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "lint-staged": {
    "src/**/*.{js,css,scss}": "prettier --single-quote --trailing-comma all --tab-width 4 --write"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-handlebars": "^8.0.2"
  },
  "devDependencies": {
    "morgan": "^1.10.1",
    "nodemon": "^3.1.10",
    "sass": "^1.93.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.4",
    "prettier": "^3.6.2"
  }
}
